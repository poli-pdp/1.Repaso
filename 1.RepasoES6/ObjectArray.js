/**
 * Object (conjunto propiedades (clave:valor)) >> {}
 */

const person = {
  nombre: "Jose",
  estado: true,
};
console.log(person);
person.edad = 50;
console.log(person);
console.log(person.nombre);

/**
 * Array conjunto de elementos (posiciones) >> []
 */

const teams = ["Nacional", "Medellin", "Envigado"];
teams.push("Rionegro");
console.log(teams);
console.log(teams[0]);
